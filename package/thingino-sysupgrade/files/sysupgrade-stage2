#!/tmp/busybox sh

export PATH="/tmp/sysupgrade"
echo "stage 2 PATH is $PATH"

echo_c() {
	echo -e "\e[38;5;$1m$2\e[0m"
}

die() {
	echo_c 160 "$1"
	exit 1
}

flash_firmware() {
	echo_c 112 "\nFlashing firmware to $mtd_dev"
	flash_eraseall $mtd_dev
	flashcp -v $fw_file $mtd_dev
}

# Write the U-Boot environment partition backed up by stage 1 straight
# back, byte-for-byte. The full flash just rewrote it with the firmware
# image's default env; this restores the exact env the camera was
# running (with its wlan_*, prov_*, ethaddr, ... keys intact).
#
# This is a raw partition copy -- no fw_setenv, no CRC computation, no
# merge. We write back bytes U-Boot already accepted, so it cannot
# synthesize a broken env or clobber bootcmd/mtdparts/gpio_*.
#
# All failure paths are non-fatal -- the upgrade proceeds to reboot.
restore_env_partition() {
	local backup="/tmp/sysupgrade/env.backup"
	local env_mtd="" line

	if [ ! -s "$backup" ]; then
		echo "No env backup at $backup; nothing to restore."
		return 0
	fi

	# Find the env partition in /proc/mtd using shell builtins only
	# (awk/grep are not on stage 2's PATH). A line looks like:
	#   mtd1: 00008000 00008000 "env"
	while IFS= read -r line; do
		case "$line" in
			mtd*'"env"')
				env_mtd="${line%%:*}"
				break
				;;
		esac
	done </proc/mtd
	if [ -z "$env_mtd" ]; then
		echo_c 208 "Env partition not found in /proc/mtd; skipping env restore."
		return 1
	fi

	echo_c 112 "\nRestoring U-Boot environment to /dev/$env_mtd"
	if flashcp -v "$backup" "/dev/$env_mtd"; then
		echo "U-Boot environment restored byte-for-byte."
		return 0
	else
		echo_c 160 "WARNING: env restore failed; camera will use the firmware default env."
		return 1
	fi
}

stop_streamer() {
	echo_c 112 "\nStopping streamer in detached stage"
	if [ -x /etc/init.d/S31raptor ]; then
		/etc/init.d/S31raptor stop
	elif [ -x /etc/init.d/S31prudynt ]; then
		/etc/init.d/S31prudynt stop
	else
		killall prudynt 2>/dev/null || true
		sleep 1
		pidof prudynt >/dev/null 2>&1 && killall -9 prudynt 2>/dev/null || true
	fi
	sleep 1
}

stream_flash_progress() {
	local logfile="$1"
	local pid="$2"
	local tail_pid=""

	if command -v tail >/dev/null 2>&1; then
		tail -n 20 -f "$logfile" &
		tail_pid=$!
	else
		echo "tail not available in PATH, monitor $logfile manually."
	fi

	wait "$pid"
	local status=$?

	if [ -n "$tail_pid" ]; then
		kill "$tail_pid" 2>/dev/null
		wait "$tail_pid" 2>/dev/null
	fi

	echo ""
	return $status
}

# Detach from terminal and protect against disconnections during flash
detach_and_flash() {
	logfile="/tmp/sysupgrade-flash.log"
	: >"$logfile"

	echo_c 208 "CRITICAL PHASE: Detaching from terminal to protect against connection loss..."
	echo "Process will continue even if SSH disconnects."
	echo "Log file: $logfile"
	echo ""

	# Run in subshell detached from terminal
	(
		# Protect against all signals during flash
		trap '' HUP INT TERM

		exec >$logfile 2>&1

		echo "=== Flash operation started at $(date) ==="
		stop_streamer

		MAX_TRIES=3
		x=0
		times=""
		while :; do
			x=$((x+1))
			if flash_firmware; then
				echo "=== Flash completed successfully at $(date) ==="
				restore_env_partition || \
					echo_c 208 "Env restore step failed; continuing to reboot anyway."
				go_reboot
				exit 1
			fi
			[ $x -gt 1 ] && times=" $x times"
			echo "Flashing failed${times}."
			[ $x -gt $MAX_TRIES ] && break
			echo "Retrying in 5 seconds..."
			sleep 5
		done
		echo "Flashing failed after $MAX_TRIES attempts!"
		echo "WARNING: You may need to perform a recovery flash restore before rebooting the camera!"
		exit 1
	) </dev/null &

	flash_pid=$!
	echo "Flash process running with PID $flash_pid (safe to disconnect)"
	echo "Streaming flash progress (Ctrl-C stops viewing, flashing continues)..."

	stream_flash_progress "$logfile" "$flash_pid"
	flash_status=$?

	if [ $flash_status -eq 0 ]; then
		echo_c 46 "Flash completed successfully. Rebooting shortly..."
		echo "Rebooting"
	else
		echo_c 160 "Flash failed; consult $logfile for details."
	fi

	exit $flash_status
}

go_reboot() {
	echo "Rebooting in 5 seconds..."
	sleep 5
	sync
	if [ -w /proc/jz/reset/reset ]; then
		echo wdt > /proc/jz/reset/reset
	fi
	sleep 2
	reboot -f
}

[ -z "$1" ] && die "Firmware file is not set."
[ -z "$2" ] && die "Target device is not set."

fw_file="$1"
mtd_dev="/dev/$2"

echo_c 208 "Running Stage 2 of sysupgrade."
echo "Firmware: $fw_file"
echo "Target: $mtd_dev"
echo ""
echo "You can still press Ctrl-C to abort before flashing starts."
echo ""

# Call the detached flash function which will protect the session
detach_and_flash
